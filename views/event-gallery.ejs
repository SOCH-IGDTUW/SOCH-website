<%- include("partials/header"); %>
<div class="gallery__top">
    <h1 data-aos="fade-right">Event Gallery</h1>
    <div data-aos="fade-right" class="underline"></div>
    <h6 data-aos="fade-right">
        Have a look at our behind the scenes and moments during events 😀
    </h6>
</div>
<div class="gallery-container">
        
    <div class="gallery">
        <% if(events.length == 0) { %> 
            Nothing to see here, come back Later. 👋
        <% } else { %> 
        <% events.forEach(event => { %>
            <a href="https://drive.google.com/uc?export=view&id=<%= event.thumbnail%>" data-lightbox="art" data-title="<%=event.title%>">
                <img src="https://drive.google.com/uc?export=view&id=<%= event.thumbnail%>">
            </a>
        <% }) }%> 
    </div>
</div>
<script src="JS/lightbox-plus-jquery.js"></script>

<%- include("partials/footer"); %>